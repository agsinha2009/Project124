function setup(){
    canvas=createCanvas(500,500);
    canvas.position(700,200);
    video=createCapture(VIDEO);
    video.size(400,400)
    video.position(200,230);
}

function draw(){
    background('#00000');
}